name: pystein-env

channels:
  - conda-forge
  - defaults

dependencies:
  - astropy
  - coverage
  - matplotlib
  - numpy
  - pip
  - pip:
      - palettable
  - pytest
  - pytest-mock
  - python=3.7
  - scipy
  - sympy
