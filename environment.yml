name: pynstein-env

channels:
  - conda-forge
  - defaults

dependencies:
  - astropy
  - coverage
  - matplotlib
  - numpy
  - pip
  - pip:
      - palettable
  - pytest
  - pytest-mock
  - scipy
  - sympy
