name: pystein-env

channels:
  - conda-forge
  - defaults

dependencies:
  - astropy
  - coverage
  - matplotlib
  - notebook
  - numpy
  - pip
  - pip:
      - palettable
  - pandas
  - pytest
  - pytest-mock
  - scipy
  - sympy
