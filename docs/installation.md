# Installation

The `pystein` package is available on PyPI, and can be installed via pip:

```bash
pip install pystein
```

Future support for conda-forge will be added and this page will be updated accordingly.
